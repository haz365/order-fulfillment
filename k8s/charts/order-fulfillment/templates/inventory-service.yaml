apiVersion: apps/v1
kind: Deployment
metadata:
  name: inventory-service
  namespace: order-fulfillment
spec:
  replicas: {{ .Values.services.inventoryService.replicas }}
  selector:
    matchLabels:
      app: inventory-service
  template:
    metadata:
      labels:
        app: inventory-service
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: {{ .Values.services.inventoryService.port | quote }}
        prometheus.io/path: "/metrics"
    spec:
      serviceAccountName: inventory-service
      securityContext:
        runAsNonRoot: true
        runAsUser: 65532
      containers:
        - name: inventory-service
          image: {{ .Values.global.registry }}/{{ .Values.services.inventoryService.image }}:{{ .Values.global.imageTag }}
          imagePullPolicy: {{ .Values.global.imagePullPolicy }}
          ports:
            - containerPort: {{ .Values.services.inventoryService.port }}
          envFrom:
            - configMapRef:
                name: app-config
          env:
            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: app-secrets
                  key: DB_PASSWORD
            - name: PORT
              value: {{ .Values.services.inventoryService.port | quote }}
          resources:
            {{- toYaml .Values.services.inventoryService.resources | nindent 12 }}
          readinessProbe:
            httpGet:
              path: /health
              port: {{ .Values.services.inventoryService.port }}
            initialDelaySeconds: 10
            periodSeconds: 5
          livenessProbe:
            httpGet:
              path: /health
              port: {{ .Values.services.inventoryService.port }}
            initialDelaySeconds: 30
            periodSeconds: 10
          startupProbe:
            httpGet:
              path: /health
              port: {{ .Values.services.inventoryService.port }}
            failureThreshold: 30
            periodSeconds: 5
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop: [ALL]
---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: inventory-service
  namespace: order-fulfillment
---
apiVersion: v1
kind: Service
metadata:
  name: inventory-service
  namespace: order-fulfillment
spec:
  selector:
    app: inventory-service
  ports:
    - port: {{ .Values.services.inventoryService.port }}
      targetPort: {{ .Values.services.inventoryService.port }}
  type: ClusterIP
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: inventory-service
  namespace: order-fulfillment
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: inventory-service
  minReplicas: {{ .Values.services.inventoryService.hpa.minReplicas }}
  maxReplicas: {{ .Values.services.inventoryService.hpa.maxReplicas }}
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: {{ .Values.services.inventoryService.hpa.targetCPU }}
---
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: inventory-service
  namespace: order-fulfillment
spec:
  minAvailable: 1
  selector:
    matchLabels:
      app: inventory-service