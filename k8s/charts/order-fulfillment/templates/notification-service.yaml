apiVersion: apps/v1
kind: Deployment
metadata:
  name: notification-service
  namespace: order-fulfillment
spec:
  replicas: {{ .Values.services.notificationService.replicas }}
  selector:
    matchLabels:
      app: notification-service
  template:
    metadata:
      labels:
        app: notification-service
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: {{ .Values.services.notificationService.port | quote }}
        prometheus.io/path: "/metrics"
    spec:
      serviceAccountName: notification-service
      securityContext:
        runAsNonRoot: true
        runAsUser: 65532
      containers:
        - name: notification-service
          image: {{ .Values.global.registry }}/{{ .Values.services.notificationService.image }}:{{ .Values.global.imageTag }}
          imagePullPolicy: {{ .Values.global.imagePullPolicy }}
          ports:
            - containerPort: {{ .Values.services.notificationService.port }}
          envFrom:
            - configMapRef:
                name: app-config
          env:
            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: app-secrets
                  key: DB_PASSWORD
            - name: PORT
              value: {{ .Values.services.notificationService.port | quote }}
          resources:
            {{- toYaml .Values.services.notificationService.resources | nindent 12 }}
          readinessProbe:
            httpGet:
              path: /health
              port: {{ .Values.services.notificationService.port }}
            initialDelaySeconds: 10
            periodSeconds: 5
          livenessProbe:
            httpGet:
              path: /health
              port: {{ .Values.services.notificationService.port }}
            initialDelaySeconds: 30
            periodSeconds: 10
          startupProbe:
            httpGet:
              path: /health
              port: {{ .Values.services.notificationService.port }}
            failureThreshold: 30
            periodSeconds: 5
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop: [ALL]
---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: notification-service
  namespace: order-fulfillment
  {{- if .Values.services.notificationService.irsaRoleArn }}
  annotations:
    eks.amazonaws.com/role-arn: {{ .Values.services.notificationService.irsaRoleArn }}
  {{- end }}
---
apiVersion: v1
kind: Service
metadata:
  name: notification-service
  namespace: order-fulfillment
spec:
  selector:
    app: notification-service
  ports:
    - port: {{ .Values.services.notificationService.port }}
      targetPort: {{ .Values.services.notificationService.port }}
  type: ClusterIP
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: notification-service
  namespace: order-fulfillment
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: notification-service
  minReplicas: {{ .Values.services.notificationService.hpa.minReplicas }}
  maxReplicas: {{ .Values.services.notificationService.hpa.maxReplicas }}
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: {{ .Values.services.notificationService.hpa.targetCPU }}
---
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: notification-service
  namespace: order-fulfillment
spec:
  minAvailable: 1
  selector:
    matchLabels:
      app: notification-service