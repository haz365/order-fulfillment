apiVersion: apps/v1
kind: Deployment
metadata:
  name: dashboard-api
  namespace: order-fulfillment
spec:
  replicas: {{ .Values.services.dashboardApi.replicas }}
  selector:
    matchLabels:
      app: dashboard-api
  template:
    metadata:
      labels:
        app: dashboard-api
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: {{ .Values.services.dashboardApi.port | quote }}
        prometheus.io/path: "/metrics"
    spec:
      serviceAccountName: dashboard-api
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
      containers:
        - name: dashboard-api
          image: {{ .Values.global.registry }}/{{ .Values.services.dashboardApi.image }}:{{ .Values.global.imageTag }}
          imagePullPolicy: {{ .Values.global.imagePullPolicy }}
          ports:
            - containerPort: {{ .Values.services.dashboardApi.port }}
          envFrom:
            - configMapRef:
                name: app-config
          env:
            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: app-secrets
                  key: DB_PASSWORD
          resources:
            {{- toYaml .Values.services.dashboardApi.resources | nindent 12 }}
          readinessProbe:
            httpGet:
              path: /health
              port: {{ .Values.services.dashboardApi.port }}
            initialDelaySeconds: 10
            periodSeconds: 5
          livenessProbe:
            httpGet:
              path: /health
              port: {{ .Values.services.dashboardApi.port }}
            initialDelaySeconds: 30
            periodSeconds: 10
          startupProbe:
            httpGet:
              path: /health
              port: {{ .Values.services.dashboardApi.port }}
            failureThreshold: 30
            periodSeconds: 5
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop: [ALL]
---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: dashboard-api
  namespace: order-fulfillment
---
apiVersion: v1
kind: Service
metadata:
  name: dashboard-api
  namespace: order-fulfillment
spec:
  selector:
    app: dashboard-api
  ports:
    - port: {{ .Values.services.dashboardApi.port }}
      targetPort: {{ .Values.services.dashboardApi.port }}
  type: ClusterIP
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: dashboard-api
  namespace: order-fulfillment
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: dashboard-api
  minReplicas: {{ .Values.services.dashboardApi.hpa.minReplicas }}
  maxReplicas: {{ .Values.services.dashboardApi.hpa.maxReplicas }}
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: {{ .Values.services.dashboardApi.hpa.targetCPU }}
---
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: dashboard-api
  namespace: order-fulfillment
spec:
  minAvailable: 1
  selector:
    matchLabels:
      app: dashboard-api