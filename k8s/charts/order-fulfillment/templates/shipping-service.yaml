apiVersion: apps/v1
kind: Deployment
metadata:
  name: shipping-service
  namespace: order-fulfillment
spec:
  replicas: {{ .Values.services.shippingService.replicas }}
  selector:
    matchLabels:
      app: shipping-service
  template:
    metadata:
      labels:
        app: shipping-service
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: {{ .Values.services.shippingService.port | quote }}
        prometheus.io/path: "/metrics"
    spec:
      serviceAccountName: shipping-service
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
      containers:
        - name: shipping-service
          image: {{ .Values.global.registry }}/{{ .Values.services.shippingService.image }}:{{ .Values.global.imageTag }}
          imagePullPolicy: {{ .Values.global.imagePullPolicy }}
          ports:
            - containerPort: {{ .Values.services.shippingService.port }}
          envFrom:
            - configMapRef:
                name: app-config
          env:
            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: app-secrets
                  key: DB_PASSWORD
          resources:
            {{- toYaml .Values.services.shippingService.resources | nindent 12 }}
          readinessProbe:
            httpGet:
              path: /health
              port: {{ .Values.services.shippingService.port }}
            initialDelaySeconds: 10
            periodSeconds: 5
          livenessProbe:
            httpGet:
              path: /health
              port: {{ .Values.services.shippingService.port }}
            initialDelaySeconds: 30
            periodSeconds: 10
          startupProbe:
            httpGet:
              path: /health
              port: {{ .Values.services.shippingService.port }}
            failureThreshold: 30
            periodSeconds: 5
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop: [ALL]
---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: shipping-service
  namespace: order-fulfillment
  {{- if .Values.services.shippingService.irsaRoleArn }}
  annotations:
    eks.amazonaws.com/role-arn: {{ .Values.services.shippingService.irsaRoleArn }}
  {{- end }}
---
apiVersion: v1
kind: Service
metadata:
  name: shipping-service
  namespace: order-fulfillment
spec:
  selector:
    app: shipping-service
  ports:
    - port: {{ .Values.services.shippingService.port }}
      targetPort: {{ .Values.services.shippingService.port }}
  type: ClusterIP
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: shipping-service
  namespace: order-fulfillment
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: shipping-service
  minReplicas: {{ .Values.services.shippingService.hpa.minReplicas }}
  maxReplicas: {{ .Values.services.shippingService.hpa.maxReplicas }}
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: {{ .Values.services.shippingService.hpa.targetCPU }}
---
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: shipping-service
  namespace: order-fulfillment
spec:
  minAvailable: 1
  selector:
    matchLabels:
      app: shipping-service